# AlphaAgent — Enterprise Python Dependencies

# UI
streamlit==1.42.0

# NIM API client
requests>=2.31.0

# NVIDIA NeMo Agent Toolkit
# nvidia-nat requires NGC private PyPI index or NVAIE license.
# Install separately: pip install nvidia-nat>=0.3.0 --extra-index-url <NGC_PYPI_URL>
# The app has a graceful fallback if nvidia-nat is not installed (see ui.py line 187).
# nvidia-nat>=0.3.0

# Vector Database (ANF PVC Backend)
pymilvus>=2.4.0

# Document Ingestion (Pre-processing for NIM Retriever)
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
pypdf>=4.0.0

# Settings management
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
tenacity>=8.0.0
boto3>=1.34.0

# Azure AD / Entra ID Authentication (OIDC)
# Required when AUTH_ENABLED=true. Uses Microsoft Authentication Library (MSAL)
# with confidential client flow for enterprise SSO.
msal>=1.28.0
